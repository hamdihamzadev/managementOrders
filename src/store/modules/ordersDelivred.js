let state = {
    smartwatch: [],
    camera: [],
    powerbank: [],
    airpods: [],
    keyboard: []
}

let mutations={
// get ===> array delivred ===> 
// get ===> array category orders ===> push every 
/**
 * if(order.category==='smartwatch'? state[smartwatch].push(order)   ) 
 */
}

let actions={

}

export default{
    namespaced:true,
    state,
    mutations,
    actions
}


